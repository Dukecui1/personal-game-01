package src.Client;

import javax.swing.*;
import java.awt.*;

public class StatusPanel extends JPanel {
    public StatusPanel() {
        super();
        this.setBounds(90, 450, 720, 360);
        this.setPreferredSize(new Dimension(720, 360));
        this.setBackground(new Color(250, 200, 50));
    }
}
